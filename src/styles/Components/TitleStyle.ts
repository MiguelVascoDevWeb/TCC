import { StyleSheet } from "react-native";

const TitleStyle = StyleSheet.create({
    title: {
        fontSize: 20,
        margin: 30,
        textAlign: 'left'
    }
});

export default TitleStyle;