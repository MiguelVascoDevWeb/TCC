import { StyleSheet } from "react-native";

const TitleStyle = StyleSheet.create({
    title: {
        fontSize: 24,
        margin: 30,
        fontWeight: 700,
    }
});

export default TitleStyle;