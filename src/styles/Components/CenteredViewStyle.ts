import { StyleSheet } from "react-native";

const CenteredViewStyle = StyleSheet.create({
    viewCentered: {
        alignItems: 'center',
        justifyContent:'center',
        width: '100%',
        
    },
});

export default CenteredViewStyle;